apiVersion: v1
kind: Pod
metadata:
  name: one
  labels:
    name: one
spec:
  containers:
  - name: one
    image: nginx
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
